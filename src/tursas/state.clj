(ns tursas.state)

(defprotocol State
  (occupied? [state index])
  (black? [state index])
  (white? [state index])
  (check? [state])
  (mate? [state])
  (draw? [state])
  (state->fen [state])
  (legal-states [state])
  (get-pieces [state])
  (apply-move [state move])
  (perft [state depth])
  (dynamic? [state])
  (full-moves [state]))
